package co.edu.uniquindio.tallerbeta.util;

/**
 * Clase que contiene constantes utilizadas en el sistema,
 * principalmente rutas de archivos para la persistencia de datos.
 */
public class Constantes {
    /** Ruta del archivo XML para almacenar datos de clientes. */
    public static final String RUTA_CLIENTE = "data/cliente.xml";
    /** Ruta del archivo XML para almacenar datos de mecánicos. */
    public static final String RUTA_MECANICO = "data/mecanico.xml";
    /** Ruta del archivo XML para almacenar datos de recepcionistas. */
    public static final String RUTA_RECEPCIONISTA = "data/recepcionista.xml";
    /** Ruta del archivo XML para almacenar datos de administradores. */
    public static final String RUTA_ADMINISTRADOR = "data/administrador.xml";
    /** Ruta del archivo XML para almacenar datos de órdenes. */
    public static final String RUTA_ORDEN = "data/orden.xml";
}